<!DOCTYPE html>
<html>
<head>
    <title>Author Details</title>
</head>
<body>
<h1>Author Details</h1>
{% if author %}
    <p>username : {{ author.username }}</p>
    <p>picture : <img  width='100'  src='{{ asset(author.picture) }}' alt="author picture" /> </p>

    <p>email : {{ author.email }}</p>
    <p>Number of books : {{ author.nb_books }}</p>

{% else %}
    <p>Auteur non trouvé.</p>
{% endif %}
</body>
</html>
<tr>{% for author in authors %}</tr>
                    <tr>
                        
                        <td>{{ author.username }}</td>
                        <td>{{ author.email }}</td>
                        <td><a href="{{ path('author_edit', {'id': author.id}) }}">EDIT</a>
                        <a href="{{ path('author_delete' , {'id': author.id}) }}">DELETE</a></td>
                        
  
                    </tr>
                {% endfor %}